repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-toml

  - repo: local
    hooks:
      - id: lint-manifests
        name: Lint agent manifests
        language: script
        entry: scripts/lint-manifests.sh
        files: ^agents/.*\.toml$
        pass_filenames: true
