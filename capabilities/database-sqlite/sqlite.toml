# capabilities/database-sqlite/sqlite.toml
# Capability: database-sqlite
# Provider: SQLite — official MCP reference server
# Title: SQLite Database
# Description: Query and manage local SQLite databases via MCP.

[deps]
require = ["modelcontextprotocol/sqlite"]

[roles.mcp]
server_refs = ["sqlite"]
allowed_tools = ["sqlite:*"]

[[mcp.servers]]
name = "sqlite"
type = "stdio"
command = "uvx"
# --with mcp<2: server uses the v1 SDK API removed in the mcp 2.x SDK.
args = ["--with", "mcp<2", "mcp-server-sqlite==2025.4.25", "--db-path", "{{ENV:SQLITE_DB_PATH}}"]
timeout_seconds = 60
tools = []
