# capabilities/core/config.toml
# Capability metadata: trigger phrases for deterministic auto-activation.
#
# core is the planning / task-decomposition meta-capability — turns a
# loosely-stated user goal into a structured, ordered plan. Triggers
# emphasize PLANNING vocabulary, NOT execution.

triggers = [
	"plan out the steps for this task",
	"break down a task into a structured plan",
	"create a step-by-step plan",
	"organize work into phases",
	"draft a plan before we start",
	"outline the approach for this project",
	"split this work into milestones",
	"sequence the subtasks for this feature",
	"think through the strategy before coding",
	"map out a roadmap for this initiative",
]
