# capabilities/docker/config.toml
# Capability metadata: trigger phrases for deterministic auto-activation.

triggers = [
	"manage docker containers",
	"build a docker image from a Dockerfile",
	"orchestrate containers with docker compose",
	"inspect docker container logs",
	"pull or push images to a docker registry",
	"manage docker volumes and networks",
]
