# capabilities/runtime/octomind.toml
# Capability: runtime
# Provider: octomind
# Title: Octomind Runtime Control
# Description: High-trust Octomind runtime controls — MCP server management, dynamic agents, and skill/capability activation (tool-surface reconfiguration).

[roles.mcp]
server_refs = ["runtime"]
allowed_tools = ["runtime:*"]

[[mcp.servers]]
name = "runtime"
type = "builtin"
timeout_seconds = 30
tools = []
