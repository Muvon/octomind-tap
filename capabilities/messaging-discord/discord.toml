# capabilities/messaging-discord/discord.toml
# Capability: messaging-discord
# Provider: mcp-discord — Discord bot integration
# Title: Discord Messaging
# Description: Discord messaging - channels, messages, moderation, and voice - via the mcp-discord server.

[deps]
require = ["discord/discord"]

[roles.mcp]
server_refs = ["discord"]
allowed_tools = []

[[mcp.servers]]
name = "discord"
type = "stdio"
command = "npx"
args = ["-y", "mcp-discord@1.3.4"]
timeout_seconds = 60
env = { DISCORD_TOKEN = "{{ENV:DISCORD_TOKEN}}" }
tools = []
