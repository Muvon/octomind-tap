# capabilities/programming-nodejs/bun.toml
# Capability: programming-nodejs
# Provider: Bun (oven-sh/bun) — modern all-in-one JS/TS runtime
# Title: Node.js Toolchain
# Description: Bun all-in-one JS/TS runtime (bun, bunx) - drop-in for node, npm, npx, yarn, pnpm, and vite.

[deps]
require = ["oven-sh/bun"]
