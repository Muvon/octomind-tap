# capabilities/programming-nodejs/node.toml
# Capability: programming-nodejs
# Provider: Node.js LTS (node, npm, npx)
# Title: Node.js Toolchain
# Description: Node.js LTS runtime with npm and npx package management.

[deps]
require = ["nodejs/node"]
