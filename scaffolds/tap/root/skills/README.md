# Skills

Skills live at `skills/<name>/SKILL.md` and follow the AgentSkills format.
The directory name must match the `name` frontmatter field. Start from
`templates/skill.md` and keep the final checklist near the end.

